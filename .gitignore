node_modules/
.env
.env.local
.env.*.local
# 設定・履歴ファイル（秘密情報含む）
.egressview.json
# MCP サーバー固有設定（EGRESSVIEW_TOKEN 等を含む）
.env.mcp
# 旧ファイル名（念のため除外）
.widemap.json
.widemap.connections.jsonl
.widemap.connections.jsonl.migrated
.widemap.db
.widemap.db-wal
.widemap.db-shm
.widemap-backups/
.widemap-upload-temp.db
.widemap.notes.json
.widemap.pid
.widemap-cert.pem
.widemap-key.pem
.egressview.connections.jsonl
.egressview.connections.jsonl.migrated
.egressview.db
.egressview.db-wal
.egressview.db-shm
.egressview-backups/
# demo DB is committed to git (WAL/SHM only excluded)
.egressview.demo.db-wal
.egressview.demo.db-shm
.egressview-upload-temp.db
.egressview.notes.json
.oui_cache.txt
server.log
*.log
# 個人的なバックログ
backlog.md
# SNS投稿用ドラフト（未公開）
SNS/
# 開発ツールのローカル設定（家庭内IP等を含む可能性）
.claude/settings.local.json
.claude/
.kiro/
# OS / エディタ
.DS_Store
.idea/
.vscode/
*.swp
*.swo
# 鍵関連（誤って置かれた場合の保険）
*.pem
*.key
id_rsa*
id_ed25519*
# ASH セキュリティスキャン（設定・出力ともローカル専用）
.ash/
# テストのベースライン（実機データに依存）
test/fixtures/baseline.json
# Playwright 生成物
test-results/
playwright-report/
.egressview.pid
local-media/
redacted-media/

# Playwright test output
test-results/
playwright-report/
# TLS 証明書（自己署名・自動生成）
.egressview-cert.pem
.egressview-key.pem
# conntrack 検証環境（内部用・公開しない）
dev-conntrack/
